# Vite proxy target (usado no vite.config.ts)
# Use http://worker:8787 dentro do Docker
# Use http://localhost:8787 fora do Docker
VITE_WORKER_URL=http://worker:8787

# URL da API em produção (Cloudflare Worker URL)
# VITE_API_URL=https://retropanning-worker.SEU-SUBDOMINIO.workers.dev

NODE_ENV=development
